"""Module-chunk retrieval via pgvector (Phase 2).

TODO(Phase 2):
- embed_chunks(module_id, chunks) -> insert into documents w/ embeddings
- chunks_for_location(campaign_id, location) -> top-k relevant chunks
"""
