"""Context builders (Phase 4). See generation-loop.md token budget.

TODO(Phase 4):
- party_sheet(campaign_id) -> str  (query the party_sheet view, format)
- build_planning_context(campaign_id)
- build_scene_context(campaign_id, episode, scene_index)
- always inject: protagonist_bible + style_guide docs verbatim
"""
