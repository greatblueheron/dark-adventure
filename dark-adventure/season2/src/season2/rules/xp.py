"""XP and leveling (Phase 1).

TODO(Phase 1):
- xp_for_encounter(monsters) + treasure XP (gold-for-XP decision)
- award(party, xp) -> per-character xp rows (prime requisite bonus)
- process_levelups(character, dice) -> new HP, spells, abilities;
  returns level_up event payloads for the events table
"""
